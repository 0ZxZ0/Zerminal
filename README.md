# Zerminal
Temnal but ez to learn
Here, the command is simpler because it uses abbreviations;
for example, instead of “sys,” it's ‘si’
(which stands for “system info”). 
You can find out more in the script itself.
