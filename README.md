# Zerminal 

Zerminal is a lightweight, custom command-line interface (CLI) environment written in Python. It allows users to navigate the file system, manage files and directories, and view system information through simple, intuitive commands. 

This project was created to practice Python development, specifically working with the `os` and `shutil` modules.

## Features
* **File Management:** Create, read, and delete files directly from the CLI.
* **Directory Management:** Create and remove folders, and scan directory trees.
* **System Info:** Instantly view your operating system, processor, and current time.
* **Custom Environment:** Displays the current working directory dynamically, just like a real terminal.

## Installation & Usage

1. **Clone the repository:**
   ```bash
   git clone [https://github.com/0ZxZ0/Zerminal.git](https://github.com/0ZxZ0/Zerminal.git)
